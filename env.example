#
# Nucleus environment variables (example)
#
# Recommended:
#   cp env.example .env
#
# Then run CLI commands from this directory. `nuc` will try to load `.env`
# (and also `env`) on startup.
#

# OpenAI API key used by `nuc intake` / `nuc desktop ai` (provider: openai.responses).
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: custom API base (defaults to https://api.openai.com)
# OPENAI_API_BASE="https://api.openai.com"

# Anthropic API key used when `--provider anthropic.messages`
ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY_HERE"

# Gemini API key used when `--provider google.gemini`
# If you already use GOOGLE_API_KEY in your environment, you can also do:
#   nuc ... --api-key-env GOOGLE_API_KEY
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"


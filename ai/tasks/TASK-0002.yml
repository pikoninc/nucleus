version: 1
id: TASK-0002
plan_id: PLAN-0001
title: builtin_desktop planner の DSL 処理の安全性確認（バリデーション/デフォルト）
status: todo # todo|doing|blocked|done
priority: P1

description: >
  `plugins/builtin_desktop/planner.py` における DSL 取り込み・デフォルト値・
  エラーメッセージを確認し、必要ならバリデーションを強化する。

acceptance_criteria:
  - 不正入力に対して明確なエラーが出る
  - 既存テストが通り、必要ならテストが追加される

scope:
  code:
    - plugins/builtin_desktop/planner.py
  tests:
    - tests/plugins/test_builtin_desktop_planner_config.py

notes:
  - 互換性が変わる場合は PLAN と TASK に変更理由を追記する

